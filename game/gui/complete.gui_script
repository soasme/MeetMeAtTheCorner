local function complete(self)
	print("complete, jump back to initial scene.")
end

function init(self)
	msg.post("#complete", "acquire_input_focus")
	msg.post("#complete", "disable")
end

function on_input(self, action_id, action)
	if action_id == hash("action") then
		complete(self)
	end
end